let should = require('should');
let testHelpers = require('../helpers');

describe('window.safeApp', () => {
  it('exists', () => {
    should.exist(window.safeApp);
  });

  it('exists', () => {
    should.exist(window.safeApp);
  });
});
